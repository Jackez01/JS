//Crie um array com os valores [10, 20, 30, 40, 50] e use .reverse para inverter a ordem dos elementos.

let numeros = [10, 20, 30, 40, 50];

numeros.reverse();

console.log(numeros);

//também da pra fazer com um loop for, reduceRight(), sort(), map(), stack